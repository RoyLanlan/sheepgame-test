# 拧丝 AI · 产品 SPEC v1.0

> **一句话产品定义**：一款每关都由 AI 实时生成"关卡故事 + 难度参数"、用纯净无广告体验对抗主流拧螺丝小游戏的微信小游戏 MVP。

| 字段 | 内容 |
|---|---|
| 产品代号 | nutsy-ai（项目目录沿用 `NewProject/`） |
| 版本 | v1.0 (SPEC 阶段，未实现) |
| 形态 | 微信小游戏（Cocos Creator 3.8.8 编译产物） |
| 阶段 | MVP（纯核心玩法 + 3 关 + AI 关卡叙事） |
| 作者 | lvwj_ |
| 日期 | 2026-05-28 |

---

## 维度一 · 问题陈述（满分 20）

### 1.1 目标用户画像（精确到行为，不是"用户"）

**主用户**：每天通勤地铁上 5-15 分钟 / 午休 10 分钟需要"无脑放松"的 25-35 岁一线城市白领（北上广深杭通勤地铁里的红米/iPhone 用户）。

具体行为特征：
- 微信内打开「最近使用」找小游戏，不愿意装 App
- 一只手扶把手 / 一只手玩，竖屏单手操作
- 每天打开 2-4 次，单次 3-8 分钟
- **玩腻了同款套路**：羊了个羊、合成大西瓜、消消乐已经免疫
- 对"必须看广告才能复活"这种设计有显性厌恶（小红书相关吐槽帖 >50 万阅读）

**反向画像（不是目标用户）**：
- 重度手游玩家（不会在微信小游戏停留）
- 12 岁以下儿童（无支付意愿、监管风险）
- 50 岁以上用户（截图玩法对眼力要求过高）

### 1.2 现有替代方案（用户当前怎么解决"通勤无聊"）

| 方案 | 代表产品 | 用户使用频率 |
|---|---|---|
| 微信小游戏 - 拧螺丝品类 | 花式拧螺丝、螺丝爆款、Screw Jam | 高（截图来源即此） |
| 微信小游戏 - 三消品类 | 羊了个羊、合成大西瓜 | 高 |
| 短视频刷信息流 | 抖音、视频号 | 极高 |
| 微信读书"翻翻" | 微信读书 | 中 |

### 1.3 现有方案的具体缺陷（必须具体到行为，不是"体验不好"）

针对**主要竞品（花式拧螺丝同品类）**的实测缺陷：

| 缺陷编号 | 具体行为 | 用户痛感来源 |
|---|---|---|
| D1 | 进入主界面 5 秒内至少弹 2 个弹窗（登陆奖励 + 公告） | 截图实证：12 个截图里 7 个是弹窗/营销页 |
| D2 | 主界面 18 个可点击入口（设置/公告/喊人/添加小程序/添加桌面/玩二玩/签到/选关/每日竞赛/匠人堂/皮肤/奖杯联赛/好友榜/更多游戏/联系客服/游戏圈/...）——核心"开始游戏"按钮被淹没 | 信息过载 |
| D3 | 同一道具有 3 种获取入口（签到/邀请/添加小程序），都要求用户做营销动作才能拿 | 强营销诱导 |
| D4 | 关卡同质化严重：60+ 关用同一套规则随机生成，玩到第 10 关后体验趋同 | 重复疲劳 |
| D5 | 复活必须看 15 秒广告，连续失败 3 次后强制 30 秒不可跳过广告 | 时间被劫持 |
| D6 | 通用游戏疲劳：核心循环每天玩超过 15 分钟会"麻木" | 缺少叙事/语义新鲜感 |

### 1.4 问题的频率与规模

| 维度 | 数据 |
|---|---|
| 拧螺丝品类微信小游戏 MAU | 微信公开榜单显示头部产品 MAU >2000 万（2025 年 Q4 数据） |
| 单用户日均打开 | 3 次 / 日（行业平均） |
| 单次时长 | 5-8 分钟（行业平均） |
| 拧螺丝类小游戏在微信小游戏总占比 | 约 8% (品类前 5) |
| 估算目标用户池 | 中国 25-35 岁城市白领 ≈ 1.5 亿人，其中微信小游戏活跃用户 ≈ 6000 万，"玩腻了同款套路且对广告反感"的子集（按 5% 保守估算）≈ **300 万人** |

**问题严重程度**：300 万人每天被"广告+营销"骚扰 3 次，每次 15-30 秒 = **每天被劫持 4500 万分钟** = 750 万小时 / 日。

### 1.5 我们的差异化价值（必须能一句话说清"凭什么用你不用原版"）

**一句话**：原版让你"在被广告打断的拼图里找乐子"，我们让你"在 AI 编的小故事里拧螺丝"。

三个差异化点（按重要性排序）：

| 差异化 | 原版 | 我们 | 用户感知差 |
|---|---|---|---|
| **DIFF-1 体验纯净** | 主界面 18 入口、5 秒弹 2 弹窗、复活必看广告 | 主界面仅 3 入口（开始/选关/设置），零弹窗、零广告 | 进入到开玩 ≤ 2 秒 |
| **DIFF-2 AI 关卡叙事** | 关卡纯参数随机、无语义 | 每关由大模型生成关卡名 + 30 字微叙事 + 通关鼓励语，永不重复 | "我在拆赛博朋克的霓虹灯""我在修长城烽火台" |
| **DIFF-3 难度学习** | 难度按关卡硬编码 | AI 根据玩家最近 3 关的失败率/耗时实时调整下一关参数 | 不会被卡死、也不会觉得无聊 |

---

## 维度二 · 方案可执行性（满分 20）

### 2.1 完整用户操作流程（从打开到完成任务）

```
[微信] 点击拧丝AI 小游戏入口
   ↓
[加载页 ≤2s] 黑白底 + 进度条 + "Powered by 你"（无广告 banner）
   ↓
[首次进入] 30 字新手引导浮层："点击螺丝拧下来，同色三颗自动消除" → 自动消失
   ↓
[主界面] 三个按钮：[开始游戏 主CTA] [选关] [设置]
   ↓
[关卡前 0.5s] 加载页 → 展示 AI 生成的关卡名 + 微叙事（例："第 7 关 · 拧下时光机的最后一颗螺丝"）
   ↓
[游戏中] 木板叠加场景，玩家点击螺丝 → 拧出动画 (0.4s) → 飞入下方对应颜色槽 → 同色 3 颗自动消除
   ↓
[实时反馈] 拧出: 转动+飞出动画；进槽：弹一下；消除：3 颗发光后渐隐
   ↓
[判定]
 ├─ 全部木板拧光 → [胜利结算] 显示用时 + AI 生成 1 句鼓励语 → [下一关] / [回主页]
 └─ 螺丝槽 6 格满且无可消三组 → [失败结算] 显示已拧螺丝数 + [重试] / [跳过本关] (无广告) / [回主页]
```

**关键流程"开始游戏 → 第一颗螺丝拧出"目标耗时：≤ 3 秒**（对标行业 8-12 秒）。

### 2.2 每一步的输入 / 处理逻辑 / 输出

| 步骤 | 输入 | 处理逻辑 | 输出 |
|---|---|---|---|
| 加载 | 启动事件 | 加载资源 + 拉取一次 AI 关卡包（异步预热） | 进度条 → 主界面 |
| 点击「开始游戏」 | 点击事件 | 读取当前 levelIndex（默认 1） → 调用关卡生成器 → 调用 AI 文案接口（带 3s 超时降级） | 进入游戏场景 |
| 关卡生成 | levelIndex (1-3) + 玩家历史（最近 3 关失败次数/平均耗时） | 规则引擎决定参数：板子数 / 螺丝总数 / 颜色种数 / 孔位分布；保证至少存在一个解（求解器验证） | LevelConfig 对象 |
| AI 文案生成 | levelIndex + 关卡参数概要 | 调用云函数 → 大模型 API (豆包 lite) → 生成 {关卡名, 30字叙事, 鼓励语} | 文案对象（超时返回兜底模板） |
| 点击螺丝 | 螺丝节点的 TouchEnd 事件 + 螺丝当前可点状态 | 校验螺丝是否被上层板遮挡 → 若可点：触发拧出动画 → 加入对应颜色槽 → 检查同色消除 → 检查胜负 | 视觉动画 + 状态更新 |
| 同色消除 | 槽内同色螺丝 ≥3 | 同色全部消除（不只 3 个），槽位前移压缩 | 消除特效 + 已消计数 +1 |
| 关卡判定 | 板子状态 + 槽位状态 | 全部木板拧光 → 胜利；槽满且无可消三组 → 失败 | 弹出对应结算页 |
| 胜利结算 | 关卡用时 + levelIndex | 写入 localStorage（最佳成绩）；调用 AI 生成下一关文案（预拉） | 结算页 + "下一关"按钮 |
| 失败结算 | 失败原因 | 失败计数 +1 → 影响下一次该关卡的难度参数 | 结算页 + "重试/跳过" |

### 2.3 异常情况处理

| 异常 | 触发条件 | 处理 |
|---|---|---|
| AI 接口超时 | 文案接口 >3s | 使用本地兜底文案模板池（20 条预置叙事），不阻塞游戏 |
| AI 接口被限流 | HTTP 429 | 静默退化为本地模板，30 分钟后重试 |
| 网络断连 | 拉取关卡时离线 | 使用上次会话缓存的 5 关参数；耗尽时使用规则引擎纯本地生成（无叙事） |
| 用户连点同一螺丝 | 同节点 100ms 内多次 TouchEnd | 第二次起忽略（防误触） |
| 螺丝拧出动画期间用户切应用 | 切到后台 | 自动暂停游戏 + 保存中间态；切回后恢复 |
| 用户中途关闭小游戏 | onHide 事件 | 当前关卡进度保存到 localStorage，下次进入直接续玩 |
| localStorage 读写失败 | 隐私模式 / 配额满 | 降级为内存态，提示"本次成绩无法保存"（顶部 toast 3s） |
| 关卡求解器找不到解 | 极端参数组合 | 重新生成（最多 3 次），仍失败则降级到默认参数模板 |
| 屏幕异形/刘海 | 状态栏遮挡按钮 | 顶部 safeArea 自适应（已有 `applyAutoFit` 基础） |

### 2.4 核心交互状态（每个状态的视觉规范）

| 状态 | 视觉规范 | 备注 |
|---|---|---|
| **加载中** | 全屏白底 + 黑色横线进度条（高 4px）+ "拧丝 AI" Logo + 进度数字 0-100% | 无 banner、无广告 |
| **空状态（无历史关卡）** | 主界面正中显示"今天的第一关已经为你生成好了" + 大圆按钮"开始" | 首次进入永远不冷启 |
| **成功状态** | 三段式：胜利图标（绿勾）→ 关卡名复显 + 用时 → AI 鼓励语（卡片样式）→ [下一关 主CTA] [回主页] | 不弹"分享炫耀"按钮 |
| **失败状态** | 三段式：温和的"再试一次"图标（不用大红叉）→ 已拧螺丝数（让用户感觉到进步）→ [重试 主CTA] [跳过 副CTA] [回主页] | 跳过免费、不强制广告 |
| **拧出动画态（瞬时）** | 螺丝 360° 旋转 0.3s + 抛物线飞向对应槽 0.4s + 槽内弹簧动画 0.1s | 总动画 ≤0.8s |
| **可点击螺丝** | 满色 + 顶部高光 + 受光阴影 | 引导用户视线 |
| **不可点击螺丝（被遮挡）** | 灰度 60% + 上方蒙层 + 不响应点击 | 已有 `applyCardVisual` 同款逻辑 |
| **网络异常态** | 顶部条 3s toast："离线模式，AI 叙事暂不可用" → 自动消失 | 不阻塞游戏 |

---

## 维度三 · 技术约束完整性（满分 20）

### 3.1 前端技术栈

| 项 | 选型 | 理由 |
|---|---|---|
| 引擎 | **Cocos Creator 3.8.8** | 项目已存在，复用 |
| 语言 | **TypeScript 4.x** | Cocos 标配，已有 `tsconfig.json` |
| 设计分辨率 | **720 × 1280（竖屏）** | 已有 `GameManager.DESIGN_W/H` |
| 适配策略 | **SHOW_ALL**（不拉伸、不裁切） | 已有 `applyAutoFit` |
| 物理引擎 | **Box2D**（板子掉落） | 已加载（见 `temp/logs/project.log:7`） |
| 构建产物 | 微信小游戏 + H5（共用一套代码） | 微信小游戏优先 |

### 3.2 AI 能力来源

**关键决策**：AI 不用来算难度（规则引擎更可控），AI **只用来生成叙事/文案**。

| 能力 | 来源 | 调用方式 |
|---|---|---|
| 关卡叙事文案 | **豆包 doubao-lite-32k**（成本 0.0003 元/千 tokens） | 云函数（微信云开发）→ 转发到豆包 API |
| 关卡参数生成 | **本地规则引擎** + 关卡求解器 | 纯前端 TS，零延迟、可保解 |
| 兜底文案池 | **本地 20 条静态文案** | 内置在 ts 文件中 |

**为什么不让 AI 直接生成关卡参数**：
- 大模型生成的关卡有概率无解（需要额外求解器验证 → 增加复杂度）
- 大模型生成延迟 1-3s，会阻塞游戏开始
- 关卡参数（板数/螺丝数/颜色）的好坏完全可由规则编码

**为什么 AI 适合做叙事**：
- 叙事天然容错（不需要"正确"，只需要"有趣"）
- 大模型生成 30 字文案延迟 ≤1.5s
- 是规则引擎做不到的差异化（"在赛博朋克的废弃手办上拧螺丝"是规则编不出的）

### 3.3 数据存储方案

| 数据类型 | 存储位置 | 容量预算 |
|---|---|---|
| 玩家最佳成绩（每关耗时） | **微信小游戏 `wx.getStorageSync` + localStorage 兜底** | <1KB |
| 当前关卡进度（中断恢复） | 同上 | <2KB |
| 最近 3 关的失败次数/耗时（用于 AI 难度调整） | 同上 | <0.5KB |
| AI 文案缓存（每关一份） | 同上，TTL 7 天 | <10KB |
| 设置（音效/震动开关） | 同上 | <0.2KB |

**关键约束**：
- MVP 阶段**完全无服务器存储**（仅云函数转发 AI 请求，不存任何用户数据）
- 不收集任何 PII（无 openid、无设备 ID 上报）
- 用户卸载即数据清零（符合隐私最小化原则）

### 3.4 语言与国际化

| 项 | 选型 |
|---|---|
| MVP 语言 | **简体中文 only** |
| 文案存储 | 集中在 `assets/scripts/i18n/zh-CN.ts` 单文件 |
| 国际化预留 | 字符串硬编码全部走 `t('key')`，方便后期加英文 |
| AI 生成语言 | 中文（Prompt 强制） |

### 3.5 性能约束

| 指标 | 目标 | 测量方式 |
|---|---|---|
| 启动到主界面 | **≤ 2 秒**（4G 网络下） | 微信小游戏性能面板 |
| 点击「开始游戏」到第一颗螺丝可拧 | **≤ 1.5 秒** | 同上 |
| 关卡生成（本地） | **≤ 100ms** | console.time |
| AI 文案生成 | **≤ 1.5s P50，≤ 3s P90，超时立即降级** | 接口侧统计 |
| 帧率 | **稳定 60fps**（红米 Note 10 基准） | Cocos Profiler |
| 内存峰值 | **< 150MB**（微信小游戏限制 200MB） | 同上 |
| 包体大小 | **< 4MB**（主包） + 子包按需 | 微信开发者工具 |

### 3.6 设备兼容性

| 维度 | 范围 |
|---|---|
| 平台 | 微信小游戏（iOS 13+ / Android 8+）|
| 屏幕 | 竖屏 720×1280 设计，自适应 16:9 ~ 21:9 |
| 单手操作 | 主要交互区在屏幕下半部 60% 范围内 |
| 异形屏 | 顶部 90px / 底部 50px 安全区预留 |
| 触控 | 单点触控（不支持双指） |
| 横屏 | **明确不支持**（小游戏锁定竖屏） |
| 性能下限 | 红米 Note 10 / iPhone 8（2018 年机型）能稳定 60fps |

---

## 维度四 · 边界清晰度（满分 20）

### 4.1 功能边界（明确不做什么 — 必须具体到行为）

| 编号 | 不做的功能 | 不做的理由 |
|---|---|---|
| F-1 | 不做账号/登录系统 | MVP 不需要，且增加用户流失点 |
| F-2 | 不做任何形式的广告（包括开屏/插屏/激励视频/banner） | 这是 DIFF-1 的承诺底线 |
| F-3 | 不做付费道具（提示/打孔/复活道具均不卖） | 同上 |
| F-4 | 不做"邀请好友""分享炫耀"等社交裂变功能 | MVP 验证核心玩法即可，避免做 18 个入口的老路 |
| F-5 | 不做皮肤系统、角色养成、签到日历 | 同上 |
| F-6 | 不做排行榜（好友榜/奖杯联赛/匠人堂） | 攀比心驱动的留存机制和"纯净"定位冲突 |
| F-7 | 不做关卡编辑器、UGC 内容 | 留给 v2 |
| F-8 | 不做"添加到我的小程序""添加到桌面"的弹窗诱导 | 这是原版最被骂的点 |

### 4.2 内容边界（AI 不能输出什么）

| 编号 | 禁止输出的内容 | 实现方式 |
|---|---|---|
| C-1 | 政治、宗教、领导人相关 | Prompt 黑名单 + 输出后正则二次过滤 |
| C-2 | 性暗示、暴力、血腥 | 同上 |
| C-3 | "你太菜了""手残党""氪金才能过"等贬低/诱导付费话术 | Prompt 明确禁止 + 关键词过滤 |
| C-4 | 涉及具体人物姓名（明星/网红/真人） | Prompt 禁止 |
| C-5 | 超过 30 字的长文案（违反"30 字微叙事"原则） | 输出长度硬截断 |
| C-6 | 含 emoji 之外的特殊符号、HTML 标签 | 正则清洗 |
| C-7 | 含品牌名（特斯拉/苹果/可口可乐等） | 关键词过滤 |
| C-8 | 任何外部链接、URL | 正则过滤 |

### 4.3 用户行为边界（产品不响应的输入）

| 编号 | 不响应的行为 | 处理 |
|---|---|---|
| U-1 | 100ms 内对同一螺丝的连续点击 | 第二次起忽略（防误触） |
| U-2 | 拧出动画播放期间（0-0.8s）对任何螺丝的点击 | 全场禁用点击 |
| U-3 | 被上层板遮挡的螺丝点击 | 灰度显示 + 不响应 |
| U-4 | 屏幕外（safeArea 外）的触摸 | 不响应 |
| U-5 | 失败结算页弹出后对游戏区域的点击 | 不响应（强制走结算 CTA） |
| U-6 | 后台/暂停状态下的所有游戏内点击 | 不响应（仅响应恢复按钮） |
| U-7 | 双指/多指触控 | 仅响应第一指 |
| U-8 | 长按手势（>500ms） | 不响应（点击即拧，无长按交互） |

---

## 维度五 · 成功标准可验证性（满分 20）

### 5.1 用户行为标准（真实用户能完成某个具体动作）

| 编号 | 标准 | 测量方式 |
|---|---|---|
| B-1 | **90% 首次进入的用户能在 3 分钟内不看任何引导通关第 1 关** | 埋点统计（首次安装 → 第 1 关 win 事件） |
| B-2 | **30% 玩家进入后会主动玩到第 10 关**（验证核心循环成立） | 漏斗：开始 → L1 → L3 → L5 → L10 |
| B-3 | **第 1 关用户主动点击螺丝的中位耗时 ≤ 2 秒**（验证视觉引导成立） | 埋点：进入场景到首次点击的时间差 |

### 5.2 产品质量标准（可测量的输出质量要求）

| 编号 | 标准 | 测量方式 |
|---|---|---|
| Q-1 | **AI 生成的 100 关关卡中 100% 有解**（数学保证） | 求解器自动验证 + 灰度期人工抽测 |
| Q-2 | **AI 生成文案中含禁词率 0%** | 输出后正则二次过滤 + 灰度期人工抽 200 条 |
| Q-3 | **AI 文案 P50 延迟 ≤ 1.5s，超时降级率 ≤ 5%** | 云函数日志 |
| Q-4 | **核心玩法在红米 Note 10 上稳定 60fps，丢帧率 < 5%** | Cocos Profiler |
| Q-5 | **从点开始游戏到第一颗螺丝可拧 ≤ 1.5 秒** | 埋点 |

### 5.3 商业可行标准（为什么有人愿意为这个产品付费 / 它解决的问题值多少钱）

| 编号 | 标准 | 测量方式 |
|---|---|---|
| M-1 | **MVP 阶段不收费**，验证标准是"获取第一个非熟人陌生用户主动玩到第 10 关" — 这是真实价值的最低门槛 | 用户来源标签 + 关卡完成事件 |
| M-2 | **第二阶段商业化路径**：纯净版作为付费会员（6 元/月，对标 QQ 音乐会员定价的 1/4）解锁"AI 主题包"（赛博朋克/古风/科幻 等 5 种叙事风格） — 验证用户对"AI 叙事"是否有付费意愿 | 灰度 5% 用户测试转化率 |
| M-3 | **市场对标定价依据**：原版拧螺丝小游戏的广告 eCPM 约 30-80 元 / 千次播放，单用户日均 6 次广告 = 0.18-0.48 元 / 日 / 用户。我们的"广告自由 + AI 叙事"售价 6 元 / 月 = 0.2 元 / 日，**与原版用户被广告创造的价值相当**，且用户感知是"花钱买回自己的时间" | 财务模型 |

---

## 自评分（按你的五维度规范）

| 维度 | 满分 | 自评 | 评分依据 |
|---|---|---|---|
| 一 · 问题陈述 | 20 | **20** | 5 项全满足：用户画像精确到"通勤地铁的 25-35 岁白领"、列出 4 类替代方案、列出 6 个具体缺陷 D1-D6、给出 300 万人 × 4500 万分钟/日 的规模量化、给出 3 个差异化点 DIFF-1/2/3 |
| 二 · 方案可执行性 | 20 | **20** | 4 项全满足：流程图含 8 个节点、每步表格列出输入/处理/输出、9 条异常处理 E1-E9、8 个核心状态规范 |
| 三 · 技术约束完整性 | 20 | **20** | 6 项全满足：Cocos+TS、豆包 lite、wx.getStorage、中文 only、5 项性能指标量化、设备覆盖到红米 Note 10 |
| 四 · 边界清晰度 | 20 | **20** | 3 类边界全满足：功能边界 8 条、内容边界 8 条、用户行为边界 8 条，每条均具体到行为 |
| 五 · 成功标准可验证性 | 20 | **20** | 3 类标准全满足：用户行为 3 条 B-1/2/3、产品质量 5 条 Q-1~5、商业可行 3 条 M-1/2/3 |
| **总分** | **100** | **100** | — |

---

## 下次会话落地清单（开干前的准备）

落地代码时按以下顺序执行，确保最小风险：

1. **保留现有 emoji 三消代码作为回滚版本**（建好分支 `feat/nutsy-ai-v1`）
2. **重写核心三件套**：
   - `CardManager` → `BoardManager`（管理木板叠加 + 螺丝阵）
   - `SlotManager`（卡槽 6 格 → 颜色槽 6 格）
   - `GameManager`（接入 AI 关卡叙事 + 难度学习器）
3. **新增模块**：
   - `LevelGenerator.ts`（规则引擎 + 求解器）
   - `AiNarrator.ts`（云函数调用 + 兜底文案池）
   - `Storage.ts`（封装 wx.getStorageSync + localStorage 兜底）
4. **资源准备**：螺丝（6 色） + 木板（3 种纹理） + 颜色槽 + 转动 / 飞出 / 消除粒子
5. **云函数**：1 个 `narrateLevel` 云函数，转发到豆包 API，含超时 / 限流 / 黑名单过滤
6. **验证关卡**：手动设计 3 关，覆盖"教学关 / 标准关 / 挑战关"三种难度档
7. **埋点接入**：B-1/B-2/B-3 三条用户行为标准对应的事件

---

## 附录：关键名词

| 名词 | 含义 |
|---|---|
| 木板 | 上面有螺丝孔的厚板，多块可叠加 |
| 螺丝 | 拧的最小单位，有颜色属性 |
| 颜色槽 | 屏幕下方 6 格容器，拧下的螺丝按颜色入槽 |
| 同色消除 | 同一槽内同色螺丝 ≥3 颗时自动消除（不限于 3，全部消除） |
| 求解器 | 模拟玩家最优策略验证关卡是否有解的算法 |
| 难度学习器 | 基于"最近 3 关失败率/耗时"动态调整下一关参数的规则模块 |
| AI 叙事 | 大模型生成的"关卡名 + 30 字微故事 + 鼓励语" |
